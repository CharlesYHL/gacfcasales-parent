$(function(){
	
});

function reset(){
	var index1 = parent.layer.getFrameIndex(window.name);
	parent.layer.close(index1)
}

function testaaaa(){
	//alert("====================");
	var index1 = parent.layer.getFrameIndex(window.name);
	parent.layer.close(index1)
}